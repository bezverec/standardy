# Build metadata. CI normally supplies these from the checked-out commit.
REGISTRY_GIT_SHA=local
SOURCE_DATE_EPOCH=0

# Cloudflare deployment (configure as GitHub Actions secrets, never commit values).
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
